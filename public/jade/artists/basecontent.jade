//- .contentzone.context(class="grow grid content-start md:content-center h-full")
- var id = response.id ;
- var clblock = (response.userData.deck || response.id == 'vision') ? '' : ' grid' ;
.contentzone.context(class="grow content-start md:content-center h-full" + clblock)
	include ./mixins 
	.content.rel.fullH.flowYauto.flowXhidden.whitetransp.mainscrollingzone(class="border-t md:border-l md:border-t-0")
		block content