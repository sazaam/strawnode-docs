include ./artists/mixins

.h-full.grid.content-center.HpadXXLg
  .sizeXXXXLg.typo.lh1 404
  .lhXLg.Tpad
    p.whitetranspTXT
      | The page
      = ' '
      strong #{response.userData.failedPath || response.path}
      = ' '
      | was not found.
    p.TmarXXLg
      if response.parentStep
        +gen_link('< Go back', '#/' + lang + response.parentStep.path + '/', '')
      else
        +gen_link('< Go home', '#/' + lang + '/', '')
